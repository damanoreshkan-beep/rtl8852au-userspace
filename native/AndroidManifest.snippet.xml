<!-- Merge into app/src/main/AndroidManifest.xml -->
<uses-feature android:name="android.hardware.usb.host" />

<application ...>
    <activity android:name=".MainActivity" android:exported="true">
        <intent-filter>
            <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
        </intent-filter>
        <meta-data
            android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
            android:resource="@xml/device_filter" />
    </activity>
</application>

<!-- res/xml/device_filter.xml : both states so it auto-launches on storage AND wifi -->
<!--
<resources>
    <usb-device vendor-id="3034" product-id="6699" />   0bda:1a2b storage
    <usb-device vendor-id="2821" product-id="6551" />   0b05:1997 wifi
</resources>
-->
