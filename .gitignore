# Realtek firmware — fetch from linux-firmware, not vendored (see README)
native/fw/*.bin
# termux-usb runtime callback the tool writes
tool/.cb.sh
tool/out*.txt
# host test binaries / build artifacts
native/test/*.o
*.o
*.so
a.out
# tool runtime artifacts: capture/replay blobs, proprietary fw, generated TX frames, logs, compiled binary
tool/*.bin
tool/*.log
tool/*.pcap
tool/*.out
tool/ax56tx_c
tool/ax56ch
# tool runtime control/state: named pipes, channel-lock, ping/TX request, launcher status
tool/*.fifo
tool/*.ctl
tool/*.ping
tool/*.status
# build scratch + local agent/skill/editor state + capture dumps
tool/.build*
tool/.table.txt
tool/.agents/
tool/.claude/
tool/skills-lock.json
captures/
